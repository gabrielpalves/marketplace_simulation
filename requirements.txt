pydantic          # For data validation (crucial for Agent JSONs)
python-dotenv     # To manage API keys
openai            # The standard client for LLM calls (works with Groq/OpenRouter)
matplotlib        # For your simulation plots
pandas            # For the transaction ledger